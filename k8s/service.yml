apiVersion: v1
kind: Service
metadata:
  name: omkekfgh
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "omkekfgh"
    port: 8080
  selector:
    app: omkekfgh
